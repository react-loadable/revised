react-loadable mordern version, which supports webpack's multiple chunks

Check the old readme [here](https://github.com/jamiebuilds/react-loadable).

New package name: `@tranvansang/react-loadable`.
